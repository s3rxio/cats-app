import { Layout } from "@/shared/ui/layout";
import { BaseComponent } from "@/shared/types";

const FavoritesPage: BaseComponent = () => {
  return <Layout>Favorites!</Layout>;
};

export default FavoritesPage;
